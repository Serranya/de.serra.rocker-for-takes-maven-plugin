language: java
after_success:
   - bash <(curl -s https://codecov.io/bash)
jdk:
   - openjdk11
script: ./mvnw clean verify jacoco:report jacoco:report-integration -Prelease,run-its
cache:
   directories:
      - $HOME/.m2
